Flask>=2.0
flask-cors
waitress
ultralytics
opencv-python-headless
numpy

# CPU PyTorch wheel - recommended to ensure CPU-only install on Render:
# This -f line points pip to PyTorch's CPU wheel index. Keep this line in the requirements file.
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html
torch==2.9.1
